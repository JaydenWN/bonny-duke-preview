---
import { getImage } from "astro:assets";
import AboutHeroImage from "../assets/blog-placeholder-about.jpg";
import Layout from "../layouts/BaseLayout.astro";
import LandingHero from "@/components/landing-hero";
import LandingServices from "@/components/landing-services";
import LandingWhy from "@/components/landing-why";
import LandingTestimonials from "@/components/landing-testimonials";
import LandingFAQ from "@/components/landing-faq";
import LandingContact from "@/components/landing-contact";

import { BadgeCheck, Cog, Home, MessagesSquareIcon, Users } from "lucide-react";
import LandingProcess from "@/components/landing-process";
import { Gallery29 } from "@/components/gallery29";

import image1 from "@images/1.JPG";
const image1opt = await getImage({ src: image1, format: "webp" });

import image2 from "@images/2.JPG";
const image2opt = await getImage({ src: image2, format: "webp" });

import image3 from "@images/3.JPG";
const image3opt = await getImage({ src: image3, format: "webp" });

import image4 from "@images/4.JPG";
const image4opt = await getImage({ src: image4, format: "webp" });

import image5 from "@images/5.JPG";
const image5opt = await getImage({ src: image5, format: "webp" });

import image8 from "@images/6.JPG";
const image8opt = await getImage({ src: image8, format: "webp" });

import image9 from "@images/7.JPG";
const image9opt = await getImage({ src: image9, format: "webp" });

import image11 from "@images/11.jpeg";
const image11opt = await getImage({ src: image11, format: "webp" });

import image12 from "@images/12.jpeg";
const image12opt = await getImage({ src: image12, format: "webp" });

import image13 from "@images/13.jpeg";
const image13opt = await getImage({ src: image13, format: "webp" });

import image14 from "@images/14.jpeg";
const image14opt = await getImage({ src: image14, format: "webp" });

import image6 from "@images/main.JPG";
const image6opt = await getImage({ src: image6, format: "webp" });

import logoImage from "@images/logo.png";
import { Instagram } from "lucide-react";
const logoImageOpt = await getImage({ src: logoImage, format: "webp" });
import TextImage from "@images/textimage.png";
---

<Layout
	title="Bonnyduke | Coming Soon"
	description="Coastal Apparel, Comming Soon. Inspired by Duke Kahanamoku and Australian Coastal Lifestyle"
>
	<Gallery29
		images={[
			image1opt.src,
			image8opt.src,
			image2opt.src,
			image14opt.src,
			image6opt.src,
			image9opt.src,
			image11opt.src,
			image4opt.src,
			image12opt.src,
			image5opt.src,
			image3opt.src,
			image13opt.src,
		]}
		logoImage={logoImageOpt.src}
		textImage={TextImage.src}
	/>
</Layout>
